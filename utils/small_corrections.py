"""
Various little mistakes that can happen during tagging and are more complicated to fix than simple replacement can be fixed here.
"""

def small_corrects(in_root):
    #in_root = fix_att_full_coverage(in_root)
    return in_root